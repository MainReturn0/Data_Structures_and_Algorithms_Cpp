#include <iostream>

using namespace std;

int main()
{

    for (int i = 0; i < 5; i++)
    {
        /* code */ cout << "Apna College" << endl;
    }

    return 0;
}

// if we put a false information in the condition it wouldnt run. and if we put infinite condition, we will get infinite loop.

// one or single run = iteration           all run = iterate.